# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/
env/

# Test / coverage caches
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# Jupyter
.ipynb_checkpoints/

# Type checkers / linters
.mypy_cache/
.ruff_cache/

# LaTeX build artifacts (manuscript/ is the canonical source tree)
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.lof
*.lot

# macOS metadata
.DS_Store

# ---------------------------------------------------------------------------
# Publication boundary — local-only, never tracked.
# Scrubbed from all history on 2026-08-12; see PUBLICATION_CHECKLIST.md.
# These paths still exist on disk. Do not `git add -f` them.
# ---------------------------------------------------------------------------

# Manuscript sources, referee reports, and response letters.
# The published article is under journal copyright; referee material is
# confidential. This repository is code-only by design.
manuscript/

# Talk slides built on the paper.
slides/

# Submission-process and internal-audit notes.
writing_comments.md
docs/JOURNAL_WORD_LIMIT.md
docs/code-audit/
docs/Gmail*.pdf

# Agent tooling config (local absolute paths, personal workflow record).
.claude/

# Retired per-repo governance artifacts (superseded by the cps store).
worklog.jsonl
WORKLOG.md
TODO_WORKFLOW.md

# Scratch output.
dummy_plot.png

# Editor backup artifacts.
*.json-e
